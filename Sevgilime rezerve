<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Esila ♡ Yusuf</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Montserrat:wght@300;500&display=swap" rel="stylesheet">
    <style>
        :root { --soft-pink: #fff0f3; --deep-red: #ff4d6d; --dark-cherry: #a71d31; }
        body { margin: 0; background: var(--soft-pink); font-family: 'Montserrat', sans-serif; color: #4a4a4a; overflow-x: hidden; }
        
        .screen { min-height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center; padding: 20px; box-sizing: border-box; transition: 0.5s ease; }

        /* 1. EKRAN: CEMAL SÜREYA ŞİİRİ */
        #intro-screen { background: white; flex-direction: column; z-index: 10; }
        .intro-card { background: #ffeef2; padding: 40px 30px; border-radius: 35px; box-shadow: 0 15px 40px rgba(255, 77, 109, 0.1); max-width: 450px; border: 1px solid #ffcad4; }
        .intro-card h2 { font-family: 'Dancing Script'; color: var(--deep-red); font-size: 2.6rem; margin-bottom: 20px; }
        .intro-card p { line-height: 1.8; font-size: 1.05rem; margin-bottom: 30px; color: #555; }

        /* 2. EKRAN: ŞİFRE EKRANI */
        #login-screen { 
            background: linear-gradient(135deg, var(--dark-cherry) 0%, #3f0d12 100%); 
            display: none; color: white; position: fixed; top: 0; left: 0; width: 100%; z-index: 20;
        }
        .login-box { width: 100%; max-width: 350px; }
        .input-group { background: rgba(255, 255, 255, 0.15); border-radius: 50px; padding: 8px; display: flex; border: 1px solid rgba(255, 255, 255, 0.3); margin-top: 30px; }
        input { background: transparent; border: none; color: white; padding: 12px; width: 100%; text-align: center; outline: none; font-size: 1.1rem; }
        input::placeholder { color: rgba(255,255,255,0.6); }

        /* 3. EKRAN: ANA İÇERİK */
        #main-content { display: none; padding: 50px 0; background: var(--soft-pink); width: 100%; }
        .container { padding: 20px; max-width: 500px; margin: auto; text-align: center; }
        .card { background: white; padding: 30px; border-radius: 30px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); margin-bottom: 30px; border-bottom: 5px solid var(--deep-red); }
        
        .photo-card { background: white; border-radius: 25px; overflow: hidden; box-shadow: 0 12px
